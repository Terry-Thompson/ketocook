<h1>Recipes</h1>

<table class="table table-striped table-bordered">
  <thead>
    <tr>
      <th>Title</th>
      <th>Description</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @recipes.each do |recipe| %>
      <tr>
        <td><%= recipe.title %></td>
        <td><%= recipe.description %></td>
        <td><%= link_to 'Show',user_recipe_path(@user, recipe), class:"btn btn-info" %></td>
        <td><%= link_to 'Edit', edit_user_recipe_path(@user,recipe), class:"btn btn-warning" %></td>
        <td><%= link_to 'Destroy',user_recipe_path(@user,recipe), method: :delete, data: { confirm: 'Are you sure?' }, class:"btn btn-danger" %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Recipe', new_user_recipe_path(@user), class:"btn btn-success" %>
